services:
  server:
    build:
      context: .
    env_file: .env
    restart: always

  client:
    build:
      context: ../voice-ai-client
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      - server
    restart: always
